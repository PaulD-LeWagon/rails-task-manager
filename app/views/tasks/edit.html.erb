<h1>Edit Task</h1>

<%= form_with model: @task do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :desc %>
  <%= t.text_field :desc %>
  <%= t.label :done, "Completed?" %>
  <%= t.check_box :done %>
  <%= t.submit %>
<% end %>

<%= link_to 'All tasks', tasks_path %>
